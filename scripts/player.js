const Deck = require("./gameDeck");
// Player logic here, will need to bring in gameDeck.js as well
("use strict");

class Player {
  constructor(name) {
    this.name = name;
    this.hand = [];
  }
}
